# Google Apps Script web app URL, from scripts/rsvp-apps-script.gs.
# Copy this file to .env.local and fill it in.
#
# Not a secret: Vite inlines every VITE_* variable into the bundle, so this
# URL ships to every visitor. Validation belongs in the Apps Script.
VITE_RSVP_ENDPOINT=
